{
  "private": "true",
  "scripts": {
    "build": "pnpm --filter '*' build",
    "test": "pnpm --filter '*' test",
    "xpreinstall": "pnpx dotsunited-merge-json ./any-toolkit/package.json ./any-toolkit-vitest/package-overrides.json > any-toolkit-vitest/package.json"
  }
}